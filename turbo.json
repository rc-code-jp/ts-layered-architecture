{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".fixme/**", "!.fixme/fixme/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
